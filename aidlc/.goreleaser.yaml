version: 2

project_name: aidlc

before:
  hooks:
    - go mod tidy

builds:
  - id: aidlc
    main: ./cmd/aidlc
    binary: aidlc
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w -X github.com/shubhangtiwari/aidlc/aidlc/internal/commands.Version={{ .Version }}

archives:
  - id: aidlc
    ids:
      - aidlc
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "arm64" }}arm64
      {{- else }}{{ .Arch }}{{ end }}
    files:
      - README.md
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: checksums.txt
  algorithm: sha256

brews:
  - name: aidlc
    repository:
      owner: shubhangtiwari
      name: homebrew-tap
    homepage: https://github.com/shubhangtiwari/aidlc
    description: Native CLI for initializing and updating AIDLC governance payloads.
    license: MIT
    install: |
      bin.install "aidlc"
    test: |
      system "#{bin}/aidlc", "version"

release:
  github:
    owner: shubhangtiwari
    name: aidlc

changelog:
  use: github
