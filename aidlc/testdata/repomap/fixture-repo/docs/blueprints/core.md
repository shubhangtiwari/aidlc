# Core

The core module formats greetings for callers.

## Public Contracts

`Greet` accepts a name and returns a display string.

## Owned State

The module owns no durable state.

## Integration Boundaries

The core module imports only the standard library.
